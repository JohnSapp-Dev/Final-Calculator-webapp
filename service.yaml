apiVersion: v1
kind: Service
metadata:
  name: calculator-web-app-service
spec:
  selector:
    app: calculator-web-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP